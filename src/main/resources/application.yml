spring:
  datasource:
    url: jdbc:mysql://localhost:3306/graphql_basic
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: false

#logging:
#  config:
#    classpath: logback-spring.xm
